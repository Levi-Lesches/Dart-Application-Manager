name: MLN Bot
version: "1.0"
systemd:
  compile: dart compile exe bin/mln_bot.dart
  executable: bin/mln_bot.exe
web-server:
  domain: mln-bot.forgot-semicolon.com
  block-crawlers: true
  reverse-proxy:
    port: 5001
    url-prefix: /api
  files-server:
    path: build/web