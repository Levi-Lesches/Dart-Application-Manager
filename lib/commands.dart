/*
dam install: Registers the current directory as a project and installs it
dam uninstall NAME: undoes all configuration and removes the app
dam forget NAME: removes the named app from the registry
dam recompile: recompile the current app and restarts the systemd service
dam list ports: lists all ports and what apps are using them
dam list domains: lists all (sub-)domains and which apps belong to them
dam list apps: lists all apps (+ version + path)
dam reload: un-installs and re-installs all managed applications
*/
